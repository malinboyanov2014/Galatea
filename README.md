# Welcome to your Expo app 👋

## Proxy usage (local development)

To run Galatea behind the shared proxy (server/server3.js), use the following steps.

1) Start the proxy
```bash
cd server
./startLocal.sh
```

2) Start the web app with the correct base path and proxy base URL
```bash
npm run web
```
